export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8000/api/v1',
  // Publisher ID de Google AdSense (p. ej. 'ca-pub-1234567890123456').
  // Vacío = no se cargan anuncios (el sitio queda igual). El ID es público.
  adsenseClient: '',
};
